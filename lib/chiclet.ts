/**
 * Category chiclet tokens — shared by the board category filter and the
 * detail-page Back link so both render the same selected-chip look.
 */
export const chicletHoverClass =
  "hover:bg-[var(--bg-footer)] dark:hover:bg-[var(--bg-footer)]"

export const chicletSelectedClass =
  "border-[var(--color-gray-dark)] bg-[var(--bg-footer)] text-foreground"
